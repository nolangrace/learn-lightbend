my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
}

akka.grpc.client {
  "inventory.InventoryService" {
    host = 127.0.0.1
    port = 8080
    use-tls = false

  }
}
